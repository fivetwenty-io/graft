#!/bin/bash
# Runs `make pre-push` (lint, golangci, trivy, test) - the same gates
# CI runs on push, so a push cannot fail CI lint without failing here
# first. Installed by `make hooks`, which points core.hooksPath at this
# directory. Bypass a single push with `git push --no-verify`.
exec make pre-push
