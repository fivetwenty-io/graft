meta:
  region: us-east-1
  az: z1
name: my-service
params:
  instance_type: t3.small
properties:
  app:
    workers: 2
    endpoint: (( concat "https://" meta.region ".example.com" ))
full_endpoint: (( grab properties.app.endpoint ))
releases:
- name: base-release
  version: 1
jobs:
- name: web
  instances: 2
